# Security

Every requirement in `handoff/design_handoff_travel_diary/SECURITY.md` has a named home.
The table below is copied verbatim from design spec §10 (which is itself the discharge
plan for `SECURITY.md`), with one column added: **Discharged in**, naming the phase (per
design spec §4's phase plan) in which the requirement becomes functionally true — not
merely when a collection field exists, but when the behaviour it describes actually runs
and can be tested. These are forward references to planned work, not placeholders: each
one names a real phase with a real, already-written scope in the design spec.

A note on the phase mapping: design spec §4 lists `otpChallenges` and `sessions` twice —
once under Phase 0 ("all collections and the first migration"), once under Phase 2
("`otpChallenges` and `sessions`" as a Phase 2 deliverable). Read together, the schema
(fields, indexes, migration) lands in Phase 0; the behaviour that makes each row's
requirement actually true — generation, comparison, rate limiting, revocation — lands in
Phase 2, which is what "Discharged in" tracks below. See the report accompanying this
task for this observation as a noted inconsistency rather than a silent assumption.

The offsite-backup row's phase (last row of the table) was not resolvable from the
design spec alone — §4's phase plan never names a phase for it — and was settled by a
controller ruling: Phase 3, because that is the first phase where both Postgres and the
media bucket hold real content worth restoring. The restore procedure itself is written
in `docs/runbook.md` starting in Phase 0; only the *demonstrated* drill is gated to
Phase 3.

| Requirement | Discharged by | Discharged in |
|---|---|---|
| OTP generated server-side with a CSPRNG | `crypto.randomInt` in `apps/web/lib/auth/otpService.ts`'s `issueChallenge`, never `Math.random`. The whole lifecycle is server-side: the prototype's browser-generated, browser-compared code is gone, not moved | **Phase 2 Task 3** |
| Only a hash stored, 5-minute expiry | `otpChallenges.codeHash` holds scrypt (`N: 16384, r: 8, p: 1`) over a fresh per-row salt — never the code (`docs/adr/0015-otp-challenge-hashing.md`). The five minutes are the domain's `EXPIRY_MS`, applied by `challengeState`; `expiresAt` is written as `createdAt + EXPIRY_MS` as a **purge index only** and is never read to decide validity, which `apps/web/lib/auth/otpService.integration.test.ts` pins by moving it a year forward and still expecting expiry | **Phase 2 Task 3** |
| Constant-time comparison, single use | `crypto.timingSafeEqual` on two equal-length derived keys in `apps/web/lib/auth/otpService.ts`'s `codeMatches`; consumption is a **conditional `UPDATE ... WHERE consumed_at IS NULL`**, so single use survives both the request that spent it and a request racing it — two simultaneous correct codes leave exactly one signed in. A timing test was attempted for the comparison and rejected as unreliable (the leak measures ~13ns behind a ~30ms scrypt derivation); it is asserted structurally instead, and the measurement is recorded in ADR 0015 | **Phase 2 Task 3** |
| Code never sent to the client in any response | `issueChallenge` returns only `{ maskedTo }`, the masked address. `apps/web/lib/auth/otpService.integration.test.ts` asserts no six-digit run appears anywhere in the serialised result, and a second case asserts none appears in what the mailer prints either (CLAUDE.md §7) | **Phase 2 Task 3** |
| Challenge bound to the session that started it | `otpChallenges.sessionHash` — SHA-256 of the pre-auth session id, indexed, and the only key `verifyChallenge` finds a row by. The column is a recorded deviation, since `DATA_MODEL.md`'s field list has nowhere to put it (`docs/deviations.md` §25). A code issued for one session and offered from another is refused as `'invalid'` — the same refusal a wrong code gets, so the error cannot say which sessions hold a live challenge | **Phase 2 Task 3** |
| `otpRequired` decided server-side | `users.otpRequired`, read from the row by `apps/web/lib/auth/signIn.ts` on **every** call — the second prototype hole, closed by there being no other value to read. `SignInRequest` has no `otpRequired` field at all: not a field that is ignored, no field. `signIn.integration.test.ts` proves that rather than assuming it, by handing `signIn` a request carrying the OPPOSITE value as an extra property — which is exactly what a client trying to force it would look like — and asserting the row still decides, in both directions; a third case flips the stored flag between two calls, so nothing can be answering from a copy taken earlier. A NULL column reads as REQUIRED: `otp_required` is nullable, and a second factor that switched itself off for a row written before the default would do it silently. Reading the flag from the request instead fails three cases. The prototype's `localStorage` read and its `storage`/`focus` listeners are **deleted, not moved** | **Phase 2 Task 5** |
| Max 3 attempts, then invalidate and force resend | The attempt is **claimed before the code is compared**, by one conditional `UPDATE ... SET attempts = attempts + 1 WHERE attempts < MAX_ATTEMPTS AND consumed_at IS NULL AND created_at > <floor>`, so the budget is enforced by Postgres rather than by this process: twelve guesses fired at once evaluate exactly three between them, and the correct code is refused afterwards (a `Promise.all` case in `apps/web/lib/auth/otpService.integration.test.ts`). `challengeState` names the refusal on the re-read. The counter is server state, not the prototype's component state, and it holds under concurrent requests as well as sequential ones | **Phase 2 Task 3** |
| Rate limit per account **and** per IP | `apps/web/lib/auth/rateLimit.ts` over a `signInAttempts` row per attempt, with the window's arithmetic in `packages/domain/src/auth/rateWindow.ts` (gated at 100%). **In Postgres, not in the process**: this app deploys to serverless invocations that do not share memory, so an in-memory `Map` would give an attacker cycling instances a fresh window each time while every local test passed (`docs/adr/0016-rate-limit-window-storage.md`, `docs/deviations.md` §27 — the collection is not in `DATA_MODEL.md`). **Counted by rank, not read-then-counted**: each request inserts its own row and is then ranked among the rows at or before it, so a burst is refused in arrival order with no lock and no check-then-write window — the direct correction of the defect ADR 0015 records. The limits `SECURITY.md` leaves unnumbered (phase ruling F27) are 20 per address per 15 minutes, counted separately on each endpoint, and 10 per account per 15 minutes on the code endpoint. Both dimensions are proved independently by real `Promise.all` bursts in `apps/web/lib/auth/rateLimit.integration.test.ts` — twenty-eight attempts from one address against twenty-eight accounts admit exactly twenty; eighteen attempts against one account from eighteen addresses admit exactly ten — and each fails when its own dimension is removed and not when the other is. **Task 5 wired it up, and wiring it up changed it.** `admitPasswordAttempt` now counts a SECOND dimension keyed on a SHA-256 of the claimed sign-in address (phase ruling F43, limit 10 per 15 minutes). Task 4 had left the password endpoint's account dimension out, assigning it to Payload's lockout — correct about the account, wrong about the endpoint: Payload can only lock a row that EXISTS, so an address naming no account met no per-address limit at all, and a dimension the miss path skipped would have been an enumeration oracle inside the limiter, on the very branch the response and the timing are made identical for. Ten rather than five so Payload's lockout still binds first for a real account, keeping one source of truth for the lockout. `apps/web/lib/auth/signIn.ts` and `apps/web/lib/auth/passwordReset.ts` are the callers; `signIn.integration.test.ts` asserts the two dimensions independently and asserts that an address naming no account records exactly the same rows as one that does. Removing the call, or the dimension, fails four cases each | **Phase 2 Task 4** (mechanism); **Task 5** (wired, plus the claimed-address dimension) |
| Account lockout with cooling-off | Payload `maxLoginAttempts: 5`, `lockTime: 15 * 60_000` on the `users` collection. `SECURITY.md` §3 assigns the per-account PASSWORD limit here, so `rateLimit.ts` deliberately keeps no second one beside it (CLAUDE.md §7). **Exercised for the first time in Task 4**, by `apps/web/collections/users.lockout.integration.test.ts`, whose load-bearing case is the CORRECT password being refused — a test that only checked wrong passwords still failing would pass with the lockout entirely absent. That test found a real defect present since Phase 0: Payload takes `lockTime` in **milliseconds** while taking `tokenExpiration`, on the same object, in seconds, so `15 * 60` had been asking for a cooling-off period of 900 milliseconds. Corrected in the same commit, and pinned by a case asserting the lock's DURATION rather than its existence. **Task 5 put it on the request path and made its refusal indistinguishable** (phase ruling F44): `signIn` returns the same `'invalid-credentials'` for a locked account as for a wrong password and an unknown address, and spends the same key derivation reaching it. A distinguishable "too many attempts" survives every other anti-enumeration measure — an attacker who cannot tell a wrong password from an unknown address can still enumerate by spending five guesses per candidate and watching for the one whose answer changes. `SCREENS.md` §3 specifies no lockout copy, so nothing in the handoff is overridden; the accepted cost, recorded rather than hidden, is that a genuinely locked-out reader is told nothing about why | **Phase 2 Task 4** (config declared in Phase 0; first proved, and corrected, here); **Task 5** (reached on the request path; its refusal made indistinguishable) |
| Resend cooldown 30s plus an hourly ceiling | `canResend` (`packages/domain/src/auth/otpChallenge.ts`) applied by `apps/web/lib/auth/otpService.ts`'s `issueChallenge`, against one query that returns both the most recent challenge and the count within the rolling hour — **counted and inserted under a per-account Postgres advisory lock**, since a count taken outside one cannot see the rows its racers have not committed. Ten simultaneous requests against an account one short of the ceiling issue one code between them, asserted by counting rows rather than by trusting the service's own answers. Thirty seconds is `RESEND_COOLDOWN_MS`; the ceiling `SECURITY.md` leaves unnumbered is `HOURLY_RESEND_CAP`, five (phase ruling F3) | **Phase 2 Task 3** |
| No user enumeration | `apps/web/lib/auth/signIn.ts`. **The response:** an unknown address, a wrong password and a locked account all return the same value, `err('invalid-credentials')`, compared whole with `toEqual` rather than by their error strings. **The timing:** Payload's local strategy derives a 512-byte PBKDF2-SHA256 key over 25,000 iterations (~40ms) before it can refuse a wrong password, and does no such work for a row it cannot find or an account already locked — `checkLoginPermission` throws first — so both of those branches would answer in about a millisecond and the identical response would be worth nothing. `burnAKey` spends an equivalent derivation on any branch that cannot reach Payload's own comparison. Measured over 25 interleaved samples per arm, compared by median: **0.90 with the derivation, 0.14 without it**, against an asserted band of 0.6–1.6 (deliberately wide — the claim is that neither branch is an order of magnitude faster, and a tight band would flake and then be deleted). The `lock_until` column is read here to decide how much WORK to do, never whether to admit: Payload remains the only authority on the lockout. Status code and headers are Task 10's to keep identical — `signIn` returns a value, not a response. **A credential store that cannot ANSWER is not a credential that is WRONG**, and `checkPassword` classifies rather than assumes: Payload signals a refusal by throwing, so an earlier bare `catch` turned a database outage into "wrong password" and would have sent the owner to reset a working password mid-incident, with nothing recorded anywhere. It now returns one of three answers, and an `'unavailable'` is reported to the operator as one fixed log line carrying nothing from the request — not the address, not a masked address, not the IP. The reader's answer is unchanged, so the property above is untouched; both directions are asserted (the operator is told, the reader is not, and an ordinary wrong password reports nothing) | **Phase 2 Task 5** |
| Reset responds identically whether or not the address exists | `apps/web/lib/auth/passwordReset.ts`. The masked address it returns is derived from the address that was SUBMITTED, never from a row, so there is one expression rather than two branches that happen to agree today; Payload's own `forgotPassword` already declines to say an address was not found. `passwordReset.integration.test.ts` compares the two answers whole, using a known and an unknown address that mask to the same string — a mask that differed between a hit and a miss would be an oracle wearing the mask meant to prevent one. The request also spends the password endpoint's windows, so the endpoint is not an unmetered way to put mail in the owner's inbox, and the link's origin is INJECTED rather than read off the request's `Host` header. **One residual is named rather than hidden: the timing.** The hit path performs a row update and a mail dispatch the miss path does not. `SECURITY.md` asks for identical timing of the sign-in miss and mismatch specifically and of this endpoint only that it "respond identically", which is discharged; closing the timing too means dispatching mail through the queue rather than inline, which is a scope boundary rather than an impossibility — its known cost is that the OTP code is sent inline too and the two paths should not diverge, so the honest version of the change moves both. **A second residual, and it is inducible:** `'delivery-failed'` is the one answer that can differ between a hit and a miss, because only a hit has a message to send. An earlier note claimed an attacker could not cause it; they can — mail providers throttle by volume, so an attacker who spends the shared password window against a candidate address, waits it out and repeats can push the account's own sending past the provider's limit, after which a real address answers `'delivery-failed'` and an invented one answers `ok`. It stays a refusal because answering `ok` would tell the owner a link is on its way when none is. What bounds it: the shared window makes the volume slow to reach, and the oracle only speaks while the owner's own mail is already failing. The same queue change closes both residuals, by reporting a failed delivery to the operator through the log rather than to the reader through the response — the shape `signIn.ts` now uses for a credential store that cannot answer. **Task 9 mounted the screens and did not add a branch to any of this.** `/admin/reset` is told one thing — whether a request was made — so there is no "no such account" state it could draw without first being handed the answer this row exists to withhold, and the masked address it prints is passed through `maskEmail` again on the way in, so a hand-typed `?sent=hello@wanderings.travel` renders as `he•••@wanderings.travel` rather than putting a whole address on a screen. The screen the link lands on is a second oracle and a deliberate one: `linkState` tells an unauthenticated caller whether any string is a live reset token, which is bounded by the 20 CSPRNG bytes Payload mints and is no more than the same caller learns by posting a password against it — and the alternative, drawing a form for a token that cannot work, is worse for the reader and no better for an attacker. `POST /admin/reset/set` carries **no CSRF check**, which is Task 10's; what that costs is bounded because the authorisation for that endpoint is the token in the body, which a cross-site forgery does not have. **One thing that endpoint did get wrong, found by the Task 9 re-review and fixed:** a `POST` with no `Content-Type` made `Request.formData()` throw before Zod saw anything, so the route answered `500` with an empty body — an unhandled error at a trust boundary. `submittedFields` now maps an unparseable body to the empty submission the schema already refuses, so it answers the same `303` to `/admin/reset` a body with no fields does, and two integration cases hold it there | **Phase 2 Task 5** (response; two residuals named); **Task 9** (the screens, and the link's own two refusals) |
| Sessions revocable, listed on the Account screen | real `sessions` rows (`docs/adr/0017-session-store-and-rotation.md`), read on every authentication by `apps/web/lib/auth/sessions.ts`'s `authenticate`, whose verdict comes from `sessionState` (`packages/domain/src/auth/session.ts`). `revokeSession` and `revokeAllSessions` back Revoke and "Sign out everywhere", and they are the **only** way to revoke: review round 1 found that revoking through a field write meant the same field could be written back, so `revokedAt` — like every other field on the collection — now refuses `update` outright (`apps/web/collections/sessions.ts`, `docs/deviations.md` §29). Every revocation case authenticates afterwards rather than asserting on `revokedAt`: a test that checks a field was written passes while nothing reads it, and one that checks it was written passes while somebody can write it back | **Phase 2 Task 6** (rows, revocation, per-user access); Phase 4 (Account screen lists them) |
| Session id rotated on login | `startSession` (`apps/web/lib/auth/sessions.ts`) is the only way to create a session and it always rotates: the identifier the browser arrived with is superseded in the **same data-modifying CTE** that mints its replacement, and a `previous` of `null` is bound into the statement rather than branched on, so there is no non-rotating path to drift. The tests assert the OLD identifier is refused rather than that a new one exists — the second is true of an implementation that never rotates. Returning the previous identifier instead fails three cases and no others | **Phase 2 Task 6** |
| Cookies `httpOnly`, `Secure`, `SameSite=Lax`, scoped to admin | `sessionCookie` (`packages/domain/src/auth/session.ts`), which `startSession` returns alongside the identifier so no caller assembles one of its own. Each of the four attributes has its own case in `session.test.ts`, so deleting one fails a test named for it rather than one comparison on an assembled string. `Path=/admin` — see the table below for what that scoping constrains | **Phase 2 Task 6** (the policy and its cookie); **Phase 2 Task 10** (setting it on responses: `POST /admin/sign-in/password` and `POST /admin/sign-in/code/verify` put `startSession`'s own `Set-Cookie` on their `303`, `POST /admin/sign-out` clears it, and `apps/web/lib/auth/browserSession.ts` mints the pre-auth identifier the same cookie carries before a reader has signed in) |
| "Keep me signed in" is a longer-lived **revocable row**, not a longer JWT | the lifetime lives in `sessions.expiresAt` (`docs/deviations.md` §30); the identifier is 32 CSPRNG bytes carrying no account, expiry or signature, so nothing about it can be believed without reading the row. Asserted from three sides: the stored column holds the long lifetime, the identifier is byte-identical in shape between a remembered and an ordinary sign-in, and a remembered row aged past its expiry stops authenticating while its untouched thirty-day cookie still says thirty days. Only a row-governed lifetime passes all three | **Phase 2 Task 6** |
| CSRF protection on cookie-authenticated mutations | `apps/web/lib/auth/adminAccess.ts`'s `isCrossSiteMutation`, applied by `apps/web/middleware.ts` to every request under `/admin` before any route file is entered: a method that is not `GET`/`HEAD`/`OPTIONS` must carry an `Origin` header equal to the request's own origin — scheme, host and port, exactly — and is answered `403` with an empty body otherwise. **`SameSite=Lax` is already set and is most of the defence, but not all of it, for two reasons worth writing down rather than assuming.** `Lax` is scoped to the SITE, so a foothold on any sibling host under the same registrable domain posts with the cookie attached; comparing full origins refuses that. And two of the mutations here are authorised by something other than the cookie — `POST /admin/reset/set` by the token in its body and `POST /admin/sign-in/password` by the credentials in its — which `SameSite` does not protect at all, so a cross-site post to the password endpoint would be a way to spend a reader's rate-limit budget from a page they are merely visiting. **An absent `Origin` is refused, not waved through**, which is the single decision that keeps this from being decorative: every browser released since 2016 sends one on a form `POST`, so a mutation without one did not come from a browser form. `middleware.test.ts` has a case named for it, and the cost — a hand-rolled client must send `Origin` — is documented in `docs/api.md` rather than discovered. `form-action 'self'` in the admin's CSP is the second half, enforced by the browser before the request leaves. **Why an origin check rather than a synchroniser token:** a token must be minted per screen, embedded in each form and stored to compare against, which for the pre-auth screens means a row for every visitor who ever loads the sign-in page; the origin check needs no state and covers a form nobody has written yet (`docs/adr/0018-admin-request-policy-and-the-guard-split.md`) | **Phase 2 Task 10** |
| A content security policy for the admin | `ADMIN_SECURITY_HEADERS` (`apps/web/lib/auth/adminAccess.ts`), set by the middleware on every `/admin` response including the `403` above: `default-src 'self'`, `base-uri 'none'`, `object-src 'none'`, `frame-ancestors 'none'`, `form-action 'self'`, `connect-src 'self'`, `font-src 'self'`, `img-src 'self' data:`, `style-src` with `'unsafe-inline'` and `script-src` with `'unsafe-inline' 'unsafe-eval'`; plus `Referrer-Policy: no-referrer`, `X-Content-Type-Options: nosniff` and `X-Robots-Tag: noindex, nofollow`. `Referrer-Policy` earns its place here more than it looks: `/admin/reset/<token>` carries a live reset token IN THE ADDRESS, and without it every outbound request from that page would put the token in a `Referer` header. **`script-src` carries `'unsafe-inline'` AND `'unsafe-eval'`, and both are measured choices rather than oversights** — Next.js's App Router streams its RSC payload as inline `<script>` elements, so a policy with neither `'unsafe-inline'` nor a per-request nonce renders a blank page; the nonce version is real, and Next's own example switches `'unsafe-eval'` on in development, which would make the strict policy the one that never runs in any local e2e, accessibility or visual run. This repository has refused that shape once already, for `sessionCookie`'s `Secure`. `'unsafe-eval'` is here for the same reason and was found the same way — by measurement, not by reading: the policy without it logged `eval() is not supported in this environment... React requires eval() in development mode` four times on every admin page under `next dev`, caught by `e2e/reset.spec.ts`'s console-error case, and React's own message says it "will never use eval() in production mode" — so omitting it would be green in CI's production build and red on every developer's machine. **It also costs nothing on top of `'unsafe-inline'`**, which is why it is added rather than switched on by environment: a policy that already permits arbitrary inline script permits arbitrary code, `eval` included. The two go together and will come out together, on the day this becomes a nonce policy. What it costs, stated plainly: this policy does not stop an injected script from RUNNING, only from loading code from, or sending anything to, an origin that is not ours. The admin renders no visitor-supplied HTML in Phase 2. Revisit when Turbopack's development build no longer needs `'unsafe-eval'`. **The diary's own headers are unaffected**, asserted one header at a time against a `/p/<n>` response, a `/m/<n>` rewrite and the 308 off the internal path | **Phase 2 Task 10** |
| Authorization on **every** mutation | Payload access control per collection **and per field** — nothing inherits trust from the page it was reached from. The per-field half is not a refinement: on `sessions`, collection-level ownership was correct and still let an owner re-point their row at another account (`user`), un-revoke a revoked session (`revokedAt`) and rewrite when it was created (`createdAt`), because each is a field inside an operation that is rightly permitted. `sessions.access.integration.test.ts` sweeps **every field the collection declares**, enumerated from the config rather than from a list, so a field added later is swept the day it lands | Phase 2 (built before the admin's ten screens exist, deliberately — see design spec §4, Phase 2); the `sessions` half is **Task 6**, corrected in review round 1 | **Phase 2 Task 10 added the HTTP half of this, and it is default-deny.** `apps/web/lib/auth/adminAccess.ts` lists the addresses that answer without a session — the steps of signing in and of getting back in — and every other address under `/admin` is guarded, including ones nobody has written yet; written the other way round, every screen Phase 4 forgot to list would be public and the failure would be invisible, because the screen works and it works for everybody. The check itself is `apps/web/lib/auth/guard.ts` and it runs in the Node server rather than the middleware, because knowing whether an identifier names a LIVE row means reading the `sessions` table and the Edge runtime cannot; a cookie-PRESENCE check would admit every revoked session, every expired one, and every anonymous visitor carrying the pre-auth identifier this surface mints into the same cookie. **What stops a Phase 4 screen forgetting the call is a test, not a layer:** `apps/web/lib/auth/adminGuardRegistration.test.ts` reads every `page.tsx` and `route.ts` under `app/(admin)/admin` off the filesystem, turns each back into the address Next serves it at, and requires each one either to be declared public or to name the guard — directly or through the single `lib/auth` module it re-exports its handler from. It fails `npm run verify` on the commit that forgets.
| Media served from a separate origin | R2 custom domain with its own restrictive CSP | Phase 1 (public diary rendering references media by URL for the first time) |
| A hidden media item stays hidden from a signed-out reader | `media`'s own `access.read` returns a `{ hidden: { not_equals: true } }` constraint rather than `true`, so the exclusion applies to `/api/media/file/<name>` as well as to a listing (`apps/web/collections/media.ts`) | **Discharged in Phase 1 Task 10**, the first task whose page displays a photograph. Asserted by `apps/web/collections/collections.integration.test.ts` — one case that a reader CAN read an ordinary item, one that they CANNOT read a hidden one, and one that a signed-in editor still can. Without a rule of its own the collection inherited Payload's default ("a logged-in user"), so every photograph in the public diary answered `403` — a failure invisible in a screenshot, since the page laid out perfectly with empty frames |
| Downloads through our handler | `GET /gallery/<slug>/download/<id>` (`apps/web/app/(diary)/gallery/[slug]/download/[id]/route.ts`), whose whole body is `apps/web/lib/readGalleryDownload.ts` and whose pure rules are `packages/domain/src/galleryDownload.ts`. Never a bucket URL: the `href` the lightbox renders is `galleryDownloadPath`'s root-relative path — no scheme, no authority, both segments percent-encoded — so it cannot resolve to `MEDIA_ORIGIN` or to any other origin. The handler serves a **derivative** (`hero`, else `frame`, else `tile`, else `thumb`; never `hero2x`, and never the uploaded original) with `Content-Disposition: attachment`, a `Content-Type` from a three-value allowlist that refuses `image/svg+xml` by name rather than by hoping the upload pipeline did, `X-Content-Type-Options: nosniff` and `X-Robots-Tag: noindex`. It verifies four things before reading a byte — the journey is published, the frame belongs to *that* journey, the frame is not `hidden`, and `allowDownload` is not `false` — and answers **one identical 404 for every refusal**, so it cannot become the enumeration oracle the requirement exists to close. A short-lived signed URL was considered and not taken (`docs/deviations.md` §17): this app already fronts the store, so signing would add an expiry to reason about without removing a hop | **Discharged in Phase 1 Task 14**, with the gallery and its lightbox. Asserted by `apps/web/lib/readGalleryDownload.integration.test.ts` (ten cases against a real Payload, a real Postgres and the real files on disk — four of them refusals, plus one that every refusal is the *same* refusal), by `packages/domain/src/galleryDownload.test.ts` (the allowlist, the derived filename, the root-relative path), and in the browser by `e2e/gallery.spec.ts`, which reads the response headers a unit test cannot see |
| `passwordProtect` gates server-side | a client-side check leaves the content fetchable | Phase 1 (diary routing) |
| `indexGalleries` respected | `robots.txt` **and** `X-Robots-Tag`, since pages are statically served | **HALF DISCHARGED; the half that reads the setting is Phase 4's, named below.** `site.indexGalleries` is a setting nothing in this repository writes or reads yet (`apps/web/globals/site.ts`) and the Settings screen that would set it is Phase 4, so no route sets `X-Robots-Tag` from it — hard-coding a directive would be inventing the policy rather than respecting the setting. What the Phase 1 final review added is the file itself: `apps/web/public/robots.txt`, static, permissive, and consistent with `indexGalleries`'s `defaultValue: true`. Serving none at all was equally permissive but by omission rather than by a decision anyone can read, and `SECURITY.md` asks for the file by name. **Phase 4 owes two things, and they are one change:** replace the static file with `apps/web/app/robots.ts` (Next's own metadata route, which can read the global) so a `false` setting produces `Disallow: /gallery/`, and set `X-Robots-Tag: noindex` on `apps/web/app/(diary)/gallery/[slug]/page.tsx` from the same setting — a static file cannot consult a database, and a `robots.txt` alone does not stop an already-known URL being indexed, which is the whole reason `SECURITY.md` asks for both. `e2e/routing.spec.ts` asserts the file is served and does not forbid the diary; the download handler beneath the gallery sets `X-Robots-Tag: noindex` unconditionally, which is not this row — a downloaded file is never a result to index, whatever the author decides about the gallery page |
| Secrets in the platform store | never in the repo; `.env` is gitignored | Phase 0 for repo hygiene (`.gitignore` already excludes `.env`, `.env.*`, keeping only `.env.example`) — moving real secrets into each provider's platform store happens as each provider is actually provisioned at deploy time, which is not pinned to a single phase in §4 |
| Offsite backups of Postgres **and** the bucket, restore tested | scheduled dump to a different provider; restore drill in `docs/runbook.md` | Phase 3. The design spec's phase plan (§4) does not itself name a phase for this operational requirement; the procedure is documented now, in Phase 0 (`docs/runbook.md`), but Phase 3 is the first point at which both Postgres and the media bucket hold real content, so it is the earliest phase where a restore drill proves anything. A demonstrated (not merely written) drill is a Phase 3 exit criterion. |

## What Task 10 did NOT close, named rather than left to be found

Three things about the HTTP boundary are worth stating in this document, because each one
is a mechanism that exists and is not (or not fully) enforcing.

- **`rateLimit.ts`'s `admitCodeAttempt` has no caller.** `POST /admin/sign-in/code/verify`
  does not call it, and the reason is structural rather than an omission: that function
  needs the `UserId` a challenge belongs to, and the account behind a challenge is not
  knowable before the code has been verified — `otpService` deliberately offers no
  session-to-account lookup, because answering one would tell an unauthenticated caller
  which browsers hold a live challenge, which is exactly what `verifyChallenge`'s single
  `'invalid'` refusal exists to prevent. What bounds guessing meanwhile is the challenge's
  own budget, enforced by Postgres rather than by this process: three attempts, claimed
  before the code is compared, single use, a five-minute life, and an hourly ceiling on how
  many challenges an account can be issued at all. What would close it is an `otpService`
  operation that names the account for a challenge WITHOUT revealing whether one exists —
  which is a change to a module gated at 100% and was not folded into the task that mounted
  the routes.
- **`POST /admin/sign-in/code/resend` is not mounted**, for the same reason: issuing a
  fresh code needs the account. `CodeStep.tsx` still draws the button, so it posts to a
  `404` (`docs/deviations.md` §33).
- **The only `MailerPort` adapter prints to a terminal.** Every one-time code and every
  reset link this surface now issues reaches the operator's console and nobody else, which
  makes the whole flow unusable by a real reader until a sending adapter lands. It is not a
  security weakness — nothing is delivered to the wrong person — but it is the difference
  between "the endpoint works" and "a reader can sign in", and `docs/runbook.md` says so.

One more thing this boundary chose, which is a cost rather than a gap: **a genuinely
rate-limited reader is told the same thing as one who mistyped a password.**
`POST /admin/sign-in/password` puts every `SignInRefusal` through one `return`, so the three
that must be indistinguishable — an unknown address, a wrong password, a locked account —
cannot be separated later by a change that only meant to distinguish `'rate-limited'` from
`'code-not-sent'`. The screen says "Those details did not let you in." for all five.

## The one cookie the admin sets

`td-session`, added in Phase 2 Task 6. It is the credential: everything the admin panel
will let somebody do rests on it.

| Property | Value |
|---|---|
| Name | `td-session` |
| Value | 32 bytes from `crypto.randomBytes`, `base64url` encoded. **Opaque** — it carries no account, no expiry and no signature, so nothing about it can be believed without reading the `sessions` row it names |
| Written by | `apps/web/lib/auth/sessions.ts`'s `startSession`, which returns the whole `Set-Cookie` value built by `sessionCookie` (`packages/domain/src/auth/session.ts`) so that no caller assembles one of its own |
| Stored as | SHA-256 of the value, hex, in `sessions.tokenHash` — indexed, and never readable through the API even by the row's owner. SHA-256 rather than scrypt for the same reason as `otpChallenges.sessionHash`: it is a lookup key, and 32 CSPRNG bytes have no dictionary to run against them (`docs/adr/0015-otp-challenge-hashing.md`) |
| Lifetime | `Max-Age` matching the row's — 12 hours ordinarily, 30 days when the reader ticks "keep me signed in". The `Max-Age` is a hint to the browser and carries no authority: `sessions.expiresAt` is what refuses, so shortening or revoking the row takes effect on the next request whatever the browser was told |
| Flags | `Path=/admin`, `HttpOnly`, `Secure`, `SameSite=Lax`. Each has its own test |
| Read by | `apps/web/lib/auth/sessions.ts`'s `authenticate`, on the server, on every admin request, through `apps/web/lib/auth/guard.ts`'s `authenticateAdminRequest` (Phase 2 Task 10). The row is read on every call and never cached, so a revoked or expired session is refused on the first request after it became so |

**`Path=/admin` constrains where the sign-in flow can live.** A browser does not send a
`Path=/admin` cookie to `/sign-in`, so a sign-in mounted outside `/admin` would set a
session it could not then read back and its "Signed in" state (`SCREENS.md` §3.4) would
never render. The screens Phase 2 Tasks 7-9 build belong at `/admin/...`. The scoping is
what keeps every public-diary request — the CDN-served pages, the media derivatives, the
gallery — carrying no credential at all, so a cache in front of the diary can never store
a response that was varied by one.

## The second cookie the admin sets, which is not a credential

`td-keep-signed-in`, added in Phase 2 Task 10. It carries ONE BIT — whether the reader
ticked "keep me signed in" — from the password step, where the checkbox is, to the code
step, where the session is actually issued and which submits six digits and nothing else.

| Property | Value |
|---|---|
| Contains | the fixed word `yes`, or nothing. Never an address, a password or a code |
| Flags | `Path=/admin`, `HttpOnly`, `Secure`, `SameSite=Lax`, `Max-Age` one hour |
| Written | by `POST /admin/sign-in/password`, **either way** — never only when ticked, so a stale `yes` from an earlier sign-in cannot lengthen this one |
| Cleared | by `POST /admin/sign-in/code/verify`, once the session it described has been issued: the choice lives in that row's own expiry from then on |
| Trusted as | nothing. It is read by exact equality, and the most a forged value achieves is a thirty-day session for an account whose password and one-time code the holder has just supplied correctly |

**Why it exists at all.** Without it the checkbox does nothing for any account with the
second factor on — which is every account by default, since `users.otpRequired` reads as
required when NULL. The alternatives were a column on `otpChallenges` (a display preference
in a table whose every other field is a credential's state) or silently ignoring the
reader's choice.

## The one cookie the public diary sets

`td-reading-surface`, added in Phase 1 Task 15. It is listed here because a cookie on a
public, unauthenticated surface is worth stating explicitly rather than leaving to be
discovered, not because `SECURITY.md` has a row for it.

| Property | Value |
|---|---|
| Name | `td-reading-surface` |
| Value | exactly one of two literal strings, `book` or `mobile` — validated by `rememberedSurface` (`packages/domain/src/readingSurface.ts`) on read, never cast |
| Written by | `apps/web/components/mobile/SurfaceCorrection.tsx`, in the browser, and only when the measured viewport disagrees with the surface the server served |
| Lifetime | the browsing session — no `Max-Age`, no `Expires` |
| Flags | `Path=/`, `SameSite=Lax`, `Secure`. `Secure` was added in Phase 2 Task 6, the phase that owns cookies; Phase 1 shipped it without, on the reasoning that it carries no secret. It carries no secret and it is not inert either — it is the one signal `apps/web/middleware.ts` reads to decide which reading surface answers, so anything able to write it chooses what a reader sees, and over plain HTTP an intermediary can. The attribute needs no environment switch: every current browser treats `http://localhost` as a secure context, so it is still set and returned in local development, and making it conditional would leave the deployed configuration the one never exercised |
| Read by | `apps/web/middleware.ts`, on the server, for `/p/<n>` and nothing else — it is the one signal that decides which of the two route entries answers (`docs/adr/0012-two-route-entries-for-two-reading-surfaces.md`). It was read by the `/p/[n]` page component until that split; the value read, and what it is read for, are unchanged. |

**What it is not.** It is not an identifier, it is not a session, it is not personal data,
it cannot be used to recognise a returning reader, and nothing else in the product reads
it. What it remembers is the width of the window in front of the reader right now — a
fact the reader's own browser measured — so that a desktop window narrowed under 860px, or
a tablet held the other way round, does not pay a round trip for the correction on every
page they turn. Its whole reason for existing is
`docs/adr/0011-two-reading-surfaces-chosen-on-the-server.md`, and if that decision is
reversed the cookie goes with it.

The middleware also reads `User-Agent`, through Next's own `userAgent()` parser, for its
device kind alone (`mobile`/`tablet`/absent). It is a first-paint guess, corrected by the
browser's own measurement, and it is neither stored nor logged.

The last row is the one `SECURITY.md` says deserves more attention than everything above
it: *"Not an attacker — losing 40GB of photographs. Automated offsite backups of
Postgres and the media bucket, on a schedule, to a different provider. Versioned or
write-once bucket storage, so a bad script or a compromised key can't delete history.
Test a restore. An untested backup is a hypothesis."* The realistic disaster for a
single-author personal blog is data loss, not intrusion, and the "Export everything"
feature on the Settings screen exists for the same reason — a genuine feature, not a
nicety.

## Threat model

Restated from `SECURITY.md`: one author, no visitor accounts, no payments, no PII beyond
the owner's email address. The realistic worst case is defacement or content loss, not
data theft. Three things in the prototype are deliberately insecure because they are
client-side interaction demos, not authentication (the OTP compared and generated in the
browser, the OTP on/off flag in `localStorage`, and the client-side three-attempt
counter) — all three are the rows above whose fix is "server-side" and "Phase 2." Two
upload risks are specific to a photo site: EXIF leaking the author's location, and an
uploaded SVG being an HTML document that becomes stored XSS. Both are discharged by the
media pipeline (Phase 3): magic-byte sniffing and SVG rejection, then EXIF read-then-
strip before storage, as detailed in `docs/data-model.md` and design spec §9.2. This runs
via the `MediaProcessor` port's `inline` adapter, in-process on Vercel, not a separate
Fly.io worker — the worker adapter is deferred until video is enabled
(`docs/adr/0004-media-pipeline-mode.md`), and both adapters run the same steps for
stills, so this discharge holds regardless of which one is bound.

# 4 · YAGNI

The detail for `CLAUDE.md` §4. `CLAUDE.md` states the rule in short and points here; the
full text — including every sentence this repository quotes as `CLAUDE.md` §4 — is below.
The section numbers are `CLAUDE.md`'s and do not change.

Build what the handoff specifies. No speculative extension points, no configuration nobody asked for, no abstraction serving a single caller. Deleting speculative code costs more than never writing it.

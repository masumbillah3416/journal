{
  "//": "../../vitest.config.ts is in the include below because apps/web/lib/docs/coverageThresholds.test.ts imports it - it reads the coverage include, exclude and threshold keys out of the config rather than restating them, and a copy would be the drift that check exists to refuse. TypeScript requires every file a project compiles to be listed, so the config joins this project (and e2e/tsconfig.json, which also reaches that test). ../../vitest.integration.config.ts is there for the same reason, from the round that extended the check to the integration pass.",
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "jsx": "preserve",
    "noEmit": true,
    "allowJs": true,
    "incremental": true,
    "plugins": [{ "name": "next" }],
    "paths": { "@payload-config": ["./payload.config.ts"] }
  },
  "include": [
    "**/*.ts",
    "**/*.tsx",
    "**/*.js",
    "**/*.jsx",
    "next-env.d.ts",
    ".next/types/**/*.ts",
    "../../vitest.config.ts",
    "../../vitest.integration.config.ts"
  ],
  "exclude": ["node_modules", ".next", "dist"]
}
